create table user(
	login_id varchar(30),
	name     varchar(30) ,
	age  int,
	sex  varchar(20),
	duty varchar(30),
	cell_number varchar(30),
	photo_url varchar(130),
	pwd varchar(30),
	used bit	
);
